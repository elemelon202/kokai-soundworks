<nav class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid">
    <%# LOGO %>
    <div class="d-flex align-items-center me-auto">
      <%= link_to root_path, class: "navbar-brand" do %>
        <%= image_tag "kokai1.png" %>
      <% end %>
      <%# USER STATUS %>
      <% if user_signed_in? %>
        <div class="ms-3 text-start">
          <p class="mb-0" style="color: #f8f9fa;">Currently signed in as: <strong><%= current_user.username %></strong> (<strong><%= current_user.user_type %>)</strong></p>
        </div>
      <% else %>
        <div class="ms-3 text-start">
          <p class="mb-0" style="color: #f8f9fa;">You <strong>are not</strong> signed in</p>
        </div>
      <% end %>
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <li class="nav-item active">
            <%= link_to "Musicians", musicians_path, class: "nav-link btn btn-primary text-white" %>
          </li>
          <li class="nav-item">
            <%= link_to "Bands", bands_path, class: "nav-link btn btn-primary text-white" %>
          </li>
          <li class="nav-item">
            <%= link_to "Venues", venues_path, class: "nav-link btn btn-primary text-white" %>
          </li>
          <li class="nav-item">
            <%= link_to posts_path, class: "nav-link btn btn-primary text-white" do %>
              <i class="fa-solid fa-newspaper"></i> Feed
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to challenges_path, class: "nav-link btn btn-primary text-white" do %>
              <i class="fa-solid fa-trophy"></i> Challenges
            <% end %>
          </li>
          <li class="nav-item dropdown">
            <a href="#" class="nav-link btn text-white dropdown-toggle" id="mainstageDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);">
              <i class="fa-solid fa-trophy"></i> MAINSTAGE
            </a>
            <div class="dropdown-menu" aria-labelledby="mainstageDropdown">
              <%= link_to mainstage_path, class: "dropdown-item" do %>
                <i class="fa-solid fa-user me-2" style="color: #fbbf24;"></i> Musicians
              <% end %>
              <%= link_to band_mainstage_path, class: "dropdown-item" do %>
                <i class="fa-solid fa-users me-2" style="color: #E936AD;"></i> Bands
              <% end %>
            </div>
          </li>
          <% if current_user.musician.present? %>
            <li class="nav-item dropdown">
              <a href="#" class="nav-link btn btn-primary text-white dropdown-toggle" id="bandDashboardDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fa-solid fa-gauge-high me-1"></i> Dashboard
              </a>
              <div class="dropdown-menu" aria-labelledby="bandDashboardDropdown">
                <h6 class="dropdown-header">Musician</h6>
                <%= link_to edit_musician_path(current_user.musician), class: "dropdown-item" do %>
                  <i class="fa-solid fa-sliders me-2"></i> View Musician Dash
                <% end %>
                <%= link_to musician_path(current_user.musician), class: "dropdown-item" do %>
                  <i class="fa-solid fa-user me-2"></i> View Musician Profile
                <% end %>
                <% if current_user.musician.bands.any? %>
                  <div class="dropdown-divider"></div>
                  <h6 class="dropdown-header">Band Dashboards</h6>
                  <% current_user.musician.bands.each do |band| %>
                    <%= link_to edit_band_path(band), class: "dropdown-item" do %>
                      <i class="fa-solid fa-users me-2"></i> <%= band.name %>
                    <% end %>
                  <% end %>
                <% end %>
              </div>
            </li>
          <% end %>
          <li class="nav-item">
            <%= render 'shared/messages_notification' %>
          </li>
          <li class="nav-item">
            <%= render 'shared/notifications' %>
          </li>
          <li class="nav-item dropdown">
            <a href="#" class="navbar-avatar-link" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <% if current_user.musician&.avatar&.attached? %>
                <%= cl_image_tag(current_user.musician.avatar.key, class: "avatar", alt: "dropdown menu") %>
              <% elsif current_user.musician.present? %>
                <div class="navbar-avatar-initials">
                  <%= current_user.musician.name.split.map(&:first).join.upcase[0..1] %>
                </div>
              <% else %>
                <div class="navbar-avatar-initials">
                  <%= current_user.username[0..1].upcase %>
                </div>
              <% end %>
            </a>
            <!-- Avatar drop down -->
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <% if current_user&.musician.present? %>
                <%= link_to "Profile", musician_path(current_user.musician), class: "dropdown-item" %>
                <%= link_to "Messages", direct_messages_path, class: "dropdown-item" %>
                <%= link_to friendships_path, class: "dropdown-item" do %>
                  <i class="fa-solid fa-user-group me-1"></i> Friends
                <% end %>
                <h6 class="dropdown-header">Bands</h6>
                <% if current_user.musician.bands.any? %>
                  <% current_user.musician.bands.each do |band| %>
                    <%= link_to "- #{band.name}", band_path(band), class: "dropdown-item" %>
                  <% end %>
                <% end %>
                <%= link_to "Create a band", new_band_path, class: "dropdown-item" %>
                <div class="dropdown-divider"></div>
              <% else %>
                <%= link_to "Create Musician Profile", new_musician_path, class: "dropdown-item" %>
                <div class="dropdown-divider"></div>
              <% end %>
              <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "btn btn-primary text-dark" %>
            <%# might need to add nav-link class later if kokai yellow is annoying %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
