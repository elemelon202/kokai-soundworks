require 'rails_helper'

RSpec.describe ApplicationCable::Connection, type: :channel do
  # Connection tests would go here if authentication is implemented in the connection
  # Currently the application cable connection is default, so minimal testing needed
end
